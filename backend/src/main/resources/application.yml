spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/safeshare_dp
    username: postgres
    password: motasem1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: safeshare.project2026@gmail.com
    password: xxjfuxtewyykvlsw
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# GEMINI AI CONFIGURATION (YAML FORMAT)
gemini:
  api:
    key: ${GCP_API_KEY}
    url: https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent